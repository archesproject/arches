define([
    'views/base-manager',
    'project-manager-data',
    'arches'
], function(BaseManagerView, data, arches) {
    var pageView = new BaseManagerView({
        viewModel: {}
    });

    return pageView;
});
