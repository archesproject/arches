## Arches 7.6.21 Release Notes

### Bug Fixes and Enhancements

-  Handles Term Search results set and nodegroup labeling issues  #[12556](https://github.com/archesproject/arches/pull/12556)
-  Make number datatype node values searchable in the main search [#12593](https://github.com/archesproject/arches/issues/12593)

-  Added a sql patch (see below) to fix the performance of tile cardinality checks against large datasets  #[12586](https://github.com/archesproject/arches/pull/12586)

-  Fixes resource-instance nodes not localizing correctly in descriptor functions. [#12539](https://github.com/archesproject/arches/pull/12539)

### Dependency changes:

```
Python:
    Upgraded:
        None
JavaScript:
    Upgraded:
        None
```

1. Upgrade to version 7.6.0 before proceeding by following the upgrade process in the [Version 7.6.0 release notes](https://github.com/archesproject/arches/blob/dev/7.6.x/releases/7.6.0.md)

1. Upgrade to Arches 7.6.21

    ```
    pip install --upgrade arches==7.6.21
    ```

1. Run the following SQL patch files from the command line
    ```
    psql -U <username> -d <database_name> -f "{path to the root of the arches install}/arches/app/db/patches/0001_v7.6_tile_cardinality_check.sql"

    ```

1. If you are running Arches on Apache, restart your server:
    ```
    sudo service apache2 reload
    ```


1. With changes in this release, numeric values are now indexed as terms allowing users to search on them from the main term search. **Re-indexing is optional**, however, numbers in resource instances saved prior to installing this patch won't be available in term search until re-indexing.

    Within your project reindex your database:

    - To run a full reindex of your database:
        ```
        python manage.py es reindex_database
        ```

    - To run a non-descructive reindex (data is never removed during the process):
        ```
        python manage.py es index_database --clear_index false
        ```
