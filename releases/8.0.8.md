## Arches 8.0.8 Release Notes

### Bug Fixes and Enhancements

- Allows profile retrieval if a user is already logged in via oauth2 token
- Ensures copy nodeid to clipboard button is available [#12532](https://github.com/archesproject/arches/pull/12532)
- Prevents postgres crash on graph publication when load_staging has many records [#12523](https://github.com/archesproject/arches/issues/12523)

### Dependency changes:

```
Python:
    Upgraded:
        None
JavaScript:
    Upgraded:
        None
```

### Upgrading Arches

1. Upgrade to version 8.0.0 before proceeding by following the upgrade process in the [Version 8.0.0 release notes](https://github.com/archesproject/arches/blob/dev/8.0.x/releases/8.0.0.md)

1. Upgrade to Arches 8.0.8
    ```
    pip install --upgrade arches==8.0.8
    ```

1. Rebuild your frontend bundle:

    - If you plan on doing active development, and need to see frontend changes automatically, run:

        ```
        npm start
        ```

    - If you do not plan to do active development, run:

        ```
        npm run build_development
        ```

    - if you are running in production, collect your static files
        ```
        python manage.py collectstatic
        ```

1. If you are running Arches on Apache, restart your server:
    ```
    sudo service apache2 reload
    ```
